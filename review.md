# Review — 2048 게임

Build 결과물(`apps/2048/index.html`, `apps/2048/style.css`, `apps/2048/script.js`)을 독립적으로 검증했다. Playwright(Chromium, `/opt/pw-browsers`)로 실제 브라우저 동작을 확인하고 코드를 리뷰했다.

## 검증 체크리스트

### 코드 리뷰
- [x] 4x4 격자 초기화 (`createEmptyBoard`, 0 = 빈 칸) — 정상
- [x] 랜덤 타일 생성 (90% `2` / 10% `4`, 시작 시 2개, 유효한 이동마다 1개) — 정상
- [x] 방향키 입력 처리 (`ArrowUp/Down/Left/Right`, `preventDefault`) — 정상
- [x] 터치 스와이프 처리 (임계값 30px, 축 비교로 방향 판정) — 정상
- [x] 밀기/합치기 로직 (`slideLine`: 압축 → 병합 → 재압축) — 정상. 병합 시 인덱스를 2칸씩 건너뛰어 **한 번의 이동에서 동일 타일이 두 번 병합되지 않음**을 확인 (예: `[2,2,2,2]` → `[4,4]`, 8로 3중 병합되지 않음)
- [x] 좌/우, 상/하 이동 시 배열 반전(reverse) 처리로 반대 방향 로직도 대칭적으로 정확히 동작
- [x] 점수 계산 (병합될 때마다 합쳐진 값 누적) — 정상
- [x] 게임 오버 판정 (빈 칸 없음 + 인접 동일값 없음) — 정상
- [x] 승리 판정 (2048 최초 등장 시 오버레이, "계속하기"로 이어서 플레이) — 정상
- [x] 재시작 버튼 (보드/점수 초기화, 최고 점수는 유지) — 정상
- [x] 최고 점수 `localStorage` 저장 (`"2048-best-score"` 키, 블로그 본체 `"theme"` 키와 충돌 없음) — 정상
- [x] 블로그 본체 파일(`index.html`, `css/`, `js/`, `posts/`, `README.md`) 미변경 — `git diff`로 확인, 변경 없음
- [x] 외부 라이브러리/CDN 미사용 — `<script>`/`<link>` 태그 전부 로컬 파일만 참조, `fetch`/외부 URL 없음

### 브라우저 동작 검증 (Playwright)
- [x] 페이지 정상 렌더링, 콘솔 에러/페이지 에러 없음
- [x] 방향키 입력 시 타일 이동/병합 및 점수 증가 확인
- [x] 터치 스와이프(4방향) 시 실제 타일 이동 확인 (`hasTouch` 컨텍스트에서 `TouchEvent` 디스패치)
- [x] 재시작 버튼 클릭 시 점수 0, 타일 2개로 초기화 확인
- [x] 최고 점수가 `localStorage`에 저장되고, 페이지 리로드 후에도 유지되며 현재 점수만 0으로 초기화됨을 확인
- [x] 모바일 뷰포트(375×667)에서 보드가 화면 폭 안에 들어오고 가로 스크롤 없음을 확인

## 발견한 문제와 수정 내역

**문제**: spec.md/build-instructions.md는 "타일 등장 시 scale 트랜지션, 병합 시 pop 애니메이션"을 요구했다. CSS에는 `tile-appear`(등장)와 `tile-pop`(`.merged` 클래스, 병합) 키프레임이 정의돼 있었지만, `script.js`의 `render()`는 매 이동마다 보드를 완전히 새로 그리면서(`innerHTML = ""` 후 재생성) **모든 타일에 무조건 `tile-appear` 애니메이션을 걸고, `.merged` 클래스는 어디에도 부여하지 않았다.** 결과적으로:
- 새로 생성된 타일뿐 아니라 기존에 있던 타일까지 이동할 때마다 계속 "등장" 애니메이션이 재생됨
- 병합(pop) 애니메이션은 코드가 전혀 실행되지 않는 죽은 CSS였음 (Playwright로 `.tile.merged` 요소가 실제로 나타나는지 확인한 결과 `false`)

**수정** (`apps/2048/script.js`, `apps/2048/style.css`만 수정, 블로그 본체 및 `spec.md`/`build-instructions.md`/`review.md`는 미수정):
1. `addRandomTile()`이 `boolean` 대신 생성된 타일의 `{r, c}` 위치를 반환하도록 변경.
2. `slideLine()`이 각 결과 인덱스가 병합으로 생성됐는지 나타내는 `mergedFlags` 배열도 함께 반환하도록 확장.
3. `move()`에서 각 행/열 처리 시 `mergedFlags`를 (반전 방향이면 함께 반전해) 4x4 `mergedCells` 불리언 행렬로 합산하고, 새로 생성된 타일 위치를 `render(mergedCells, newTiles)`로 전달.
4. `render()`가 병합된 칸에는 `merged` 클래스를, 새로 생성된 타일에는 `tile-new` 클래스를 부여하도록 수정.
5. `style.css`: 기본 `.tile`에 걸려 있던 무조건적 `tile-appear` 애니메이션을 제거하고 `.tile.tile-new`에만 적용되도록 분리. `.tile.merged`(pop)는 기존대로 유지.

수정 후 Playwright로 재검증한 결과, 실제 플레이 중 `.tile.merged`와 `.tile.tile-new` 클래스가 모두 정상적으로 나타남을 확인했다.

그 외에는 코드 리뷰 및 브라우저 테스트에서 추가로 발견된 버그나 요구사항 미충족 항목이 없었다.

## 최종 결론

**정상 동작 확인.** 위에서 발견한 애니메이션 관련 구현 누락(병합 pop 애니메이션 미적용)을 `apps/2048/` 폴더 내 파일만 수정해 해결했으며, 이후 재실행한 Playwright 테스트(키보드 입력, 스와이프, 재시작, localStorage 최고 점수 유지, 모바일 375×667 레이아웃, 콘솔/페이지 에러 없음)에서 모든 항목이 통과했다. 블로그 본체 파일은 전혀 수정되지 않았고 외부 라이브러리/CDN도 사용하지 않았다. 남은 이슈 없음.
