# 2048 게임 — 구현 계획 (spec.md)

## 1. 웹앱 개요

- **이름**: 2048
- **설명**: 4x4 격자에서 방향키(↑↓←→)로 숫자 타일을 밀어 같은 숫자끼리 합치는 퍼즐 게임. 두 타일이 합쳐지면 값이 두 배가 되고 점수에 더해진다. 2048 타일을 만들면 승리, 더 이상 이동할 수 없으면 게임 오버.
- **입력 방식**: 데스크톱은 키보드 방향키, 모바일은 터치 스와이프(상/하/좌/우)를 함께 지원한다.
- **점수판**: 현재 점수(현재 게임 진행 중 누적 점수)와 최고 점수(localStorage에 영구 저장, 게임을 새로 시작해도 유지)를 함께 표시한다.
- **자체 완결 원칙**: 블로그 본체(`index.html`, `css/style.css`, `js/*.js`)는 이번 Build 단계에서 건드리지 않는다. `/apps/2048/` 폴더 안에서만 모든 리소스(HTML/CSS/JS)를 자체적으로 로드한다.

## 2. 파일 구조

```
apps/
└── 2048/
    ├── index.html      # 게임 페이지 (게임 보드 + 점수판 + 재시작 버튼 마크업)
    ├── style.css        # 앱 전용 스타일 (그리드, 타일 색상, 애니메이션, 반응형)
    └── script.js        # 게임 로직 (상태 관리, 입력 처리, 렌더링, localStorage)
```

- 외부 이미지/폰트/아이콘 파일은 필요 없다. 자체 완결 원칙상 파비콘은 생략한다.
- 앱 하나당 파일 3개(HTML/CSS/JS)로 최소 구성. script.js 하나에서 관리하는 것으로 충분한 규모다(약 200~300줄 예상).

## 3. 핵심 기능 목록

1. **보드 초기화**: `board`를 4x4 2차원 배열(`Array(4).fill(null).map(() => Array(4).fill(0))`)로 표현. `0`은 빈 칸.
2. **랜덤 타일 생성**: 게임 시작 시 빈 칸 중 2곳에 타일 생성(90% 확률로 `2`, 10% 확률로 `4`). 이후 유효한 이동(보드에 변화가 생긴 이동)이 일어날 때마다 빈 칸 중 1곳에 새 타일 1개 추가.
3. **방향키 입력 처리**: `keydown` 이벤트로 `ArrowUp/Down/Left/Right` 감지. 스크롤 등 기본 동작은 `preventDefault()`로 막는다.
4. **터치 스와이프 처리**: `touchstart`의 시작 좌표와 `touchend`의 종료 좌표 차이를 계산해, 일정 임계값(예: 30px) 이상 이동했을 때 더 큰 축(x/y)을 기준으로 방향을 판정한다.
5. **밀기/합치기 로직**: 방향별로 각 행(좌우 이동) 또는 열(상하 이동)에 대해 다음을 수행.
   - 0을 제거해 압축(compact)
   - 인접한 같은 값을 병합(merge) — 한 번의 이동에서 이미 병합된 타일은 다시 병합되지 않도록 처리
   - 병합 후 다시 압축
   - 이동 전후 보드를 비교해 실제로 변화가 있었는지 판정(변화 없으면 새 타일 생성 및 게임오버 판정을 건너뜀)
6. **점수 계산**: 병합이 일어날 때마다 합쳐진 값을 현재 점수에 더한다.
7. **게임 오버 판정**: 보드에 빈 칸이 없고, 상하좌우 어느 방향으로도 인접한 동일 값 쌍이 없으면 게임 오버 오버레이를 표시한다.
8. **승리 판정**: 보드에 `2048` 타일이 처음 등장하면 "You Win" 오버레이를 표시하되, "계속하기" 버튼으로 게임을 이어갈 수 있게 한다.
9. **재시작 버튼**: 클릭 시 보드와 현재 점수를 초기화하고 새 타일 2개를 생성한다. 최고 점수는 초기화하지 않는다.
10. **최고 점수 localStorage 저장**: 현재 점수가 저장된 최고 점수를 넘어설 때마다 즉시 갱신 및 저장한다. 키 이름은 `"2048-best-score"`처럼 접두어를 붙여 블로그 본체의 `"theme"` 키와 충돌하지 않게 한다.

## 4. UI/UX 고려사항

- **모바일 대응**: `viewport` 메타 태그 설정, 터치 스와이프 지원, 버튼/타일 터치 타깃 충분히 크게(최소 40px 이상).
- **반응형 레이아웃**: 게임 보드는 CSS Grid(4x4, `aspect-ratio: 1 / 1`)로 구성하고, 보드 전체 너비를 `min(90vw, 420px)` 같은 방식으로 제한한다. 점수판(현재 점수/최고 점수)은 보드 위에 flex로 배치.
- **타일 색상/애니메이션**: 값별 배경색 매핑(2, 4, 8, 16, ... 2048+)을 `data-value` 속성 기반으로 지정. 타일 등장 시 `scale` 트랜지션, 병합 시 `pop` 애니메이션을 순수 CSS로 구현.
- **다크모드**: 앱은 자체 완결되어야 하므로 블로그 본체의 `js/theme.js`에 의존하지 않고, `style.css` 자체 CSS 변수 + `@media (prefers-color-scheme: dark)`로 다크모드를 지원한다.
- **접근성**: 점수 변경 영역에 `aria-live="polite"` 부여. 재시작 버튼은 키보드로 포커스/조작 가능. 타일 색상은 명도 대비 확보.

## 5. 외부 라이브러리 사용 여부

- **사용하지 않는다.** 게임 로직(격자 배열 조작, 키보드/터치 이벤트, localStorage)은 바닐라 JavaScript만으로 충분히 구현 가능하므로 별도 라이브러리나 CDN 스크립트를 추가하지 않는다.

## 6. 블로그 메인 페이지 임베드 방식 계획 (Embed 단계에서 실제 반영)

- `index.html`의 `<main>` 내부(글 목록 위)에 "Mini Apps" 섹션을 신설한다.
- 카드 UI는 기존 `.post-card` 스타일 패턴(둥근 모서리, 카드 배경, hover 효과)을 재사용하는 `.app-card` 클래스로 구성해 디자인 일관성을 유지한다.
- 카드에는 제목("2048"), 한 줄 설명("방향키로 숫자 타일을 밀어서 합치는 퍼즐 게임"), `apps/2048/index.html`로 이동하는 링크를 넣는다. 무거운 iframe 실시간 미리보기 대신 정적 텍스트 카드로 시작한다.
- 앱이 1개뿐인 현재 시점에는 `index.html`에 카드를 직접 하드코딩한다.
- 이 섹션은 계획이며, `index.html` / `css/style.css` 수정은 Review 통과 후 Embed 단계에서 진행한다.
