# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
글쓰기는 마크다운으로 하고, 이를 읽기 좋은 블로그 포스트 페이지로 렌더링합니다.

## 기술 스택 / 제약사항

- **순수 HTML, CSS, JavaScript만 사용.** React, Vue, Next.js, Svelte 등 프론트엔드 프레임워크는 사용하지 않는다.
- 번들러(Webpack, Vite 등)나 별도 빌드 파이프라인을 강제하지 않는다. 파일을 열면 바로 동작하는 구조를 우선한다.
- 마크다운 파싱은 외부 라이브러리에 의존하지 않고 직접 구현한 경량 파서를 사용한다. (제목, 문단, 리스트, 코드블록, 링크, 이미지, 굵게/기울임 등 기본 문법 지원이면 충분)
- 의존성을 추가하기 전에는 반드시 정말 필요한지 먼저 검토한다.

## 아키텍처

- `posts/` — 블로그 글 원본 마크다운 파일들 (예: `2026-08-19-post-title.md`)
- `index.html` — 글 목록을 보여주는 메인 페이지
- `post.html` — 개별 글을 렌더링하는 템플릿 페이지 (쿼리 파라미터나 라우팅으로 어떤 글을 보여줄지 결정)
- `css/` — 스타일시트 (라이트/다크 테마 포함)
- `js/` — 마크다운 파서, 글 목록 로더, 렌더링 로직, 다크모드 토글 등
- 글 메타데이터(제목, 날짜, 요약)는 `posts/posts.json` 인덱스 파일로 관리한다. 글을 추가할 때는 `posts/<slug>.md` 파일과 `posts.json` 항목을 함께 추가해야 한다.
- `fetch()`로 마크다운/JSON을 불러오므로 `http(s)://` 환경(로컬 정적 서버 또는 GitHub Pages)에서 열어야 동작한다. `file://`로 직접 여는 것은 지원하지 않는다.

## 디자인 가이드라인

- **깔끔하고 읽기 좋은 디자인**을 최우선으로 한다. 본문 가독성(줄 간격, 본문 너비 `65-75ch` 내외, 여백)에 신경 쓴다.
- **다크 모드 지원 필수.** `prefers-color-scheme` 미디어쿼리로 시스템 설정을 기본 반영하고, 사용자가 수동으로 토글할 수 있게 한다 (선택은 `localStorage`에 저장).
- 색상, 폰트 크기 등은 CSS 커스텀 프로퍼티(`:root` 변수)로 정의해서 라이트/다크 테마 전환이 쉽게 되도록 한다.
- **모바일 반응형 필수.** 뷰포트 메타 태그, 유동적인 레이아웃(flexbox/grid), 상대 단위(rem, %, vw) 사용. 작은 화면에서 폰트/여백이 깨지지 않는지 항상 확인한다.
- 폰트는 시스템 폰트 스택을 기본으로 하되, 필요 시 웹폰트는 최소한으로만 추가한다.
- 코드 블록에는 가독성 좋은 monospace 폰트와 다크모드에서도 대비가 적절한 배경색을 사용한다.

## 작업 방식

- UI/디자인 변경 후에는 반드시 브라우저에서 라이트/다크 모드, 데스크톱/모바일 크기를 직접 확인한다.
- 불필요한 추상화나 미래를 위한 과도한 설계를 피하고, 지금 필요한 기능만 구현한다.
- 주석은 "왜"가 비직관적일 때만 최소한으로 작성한다.
