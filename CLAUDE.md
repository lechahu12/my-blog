# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
글쓰기는 마크다운으로 하고, 이를 읽기 좋은 블로그 포스트 페이지로 렌더링합니다.

## 기술 스택 / 제약사항

- **순수 HTML, CSS, JavaScript만 사용.** React, Vue, Next.js, Svelte 등 프론트엔드 프레임워크는 사용하지 않는다.
- 번들러(Webpack, Vite 등)나 별도 빌드 파이프라인을 강제하지 않는다. 파일을 열면 바로 동작하는 구조를 우선한다.
- 마크다운 파싱은 외부 라이브러리에 의존하지 않고 직접 구현한 경량 파서를 사용한다. (제목, 문단, 리스트, 코드블록, 링크, 이미지, 굵게/기울임 등 기본 문법 지원이면 충분)
- 의존성을 추가하기 전에는 반드시 정말 필요한지 먼저 검토한다.

## 아키텍처

- `posts/` — 블로그 글 원본 마크다운 파일들 (예: `2026-08-19-post-title.md`)
- `index.html` — 글 목록을 보여주는 메인 페이지
- `post.html` — 개별 글을 렌더링하는 템플릿 페이지 (쿼리 파라미터나 라우팅으로 어떤 글을 보여줄지 결정)
- `css/` — 스타일시트 (라이트/다크 테마 포함)
- `js/` — 마크다운 파서, 글 목록 로더, 렌더링 로직, 다크모드 토글 등
- 글 메타데이터(제목, 날짜, 요약, 태그)는 `posts/posts.json` 인덱스 파일로 관리한다. 글을 추가할 때는 `posts/<slug>.md` 파일과 `posts.json` 항목을 함께 추가해야 한다. `tags` 필드는 스키마상 미리 준비된 것이며, 글 5개 이상 + 반복 태그 2개 이상이 되기 전까지 UI는 만들지 않는다 (아래 "기능 추가 시점" 참고).
- `favicon.svg`(직접 제작, 외부 의존성 없음)를 루트에 둔다.
- `fetch()`로 마크다운/JSON을 불러오므로 `http(s)://` 환경(로컬 정적 서버 또는 GitHub Pages)에서 열어야 동작한다. `file://`로 직접 여는 것은 지원하지 않는다.

## 디자인 가이드라인

- **깔끔하고 읽기 좋은 디자인**을 최우선으로 한다. 본문 너비는 `--content-width: 42rem`으로 고정한다 — `ch` 단위는 라틴 `"0"` 글리프 기준이라 폰트 폴백에 따라 실제 폭이 달라지므로, 한글 기준 줄당 약 40자(WCAG 1.4.8 CJK 권장치)가 나오도록 계산한 고정값을 쓴다.
- 헤딩에 세리프 폰트를 도입하지 않는다. Windows/macOS 전반에서 신뢰할 만한 현대적 한글 세리프 시스템 폰트가 없어 조용히 산세리프로 폴백되거나 웹폰트 추가가 필요해지기 때문. "에디토리얼한" 느낌은 기존 산세리프 스택 안에서 `font-weight`/`letter-spacing`으로만 표현한다.
- **다크 모드 지원 필수.** `prefers-color-scheme` 미디어쿼리로 시스템 설정을 기본 반영하고, 사용자가 수동으로 토글할 수 있게 한다 (선택은 `localStorage`에 저장). `<html>`에 `color-scheme: light dark`를 함께 지정해서 스크롤바·폼 컨트롤 같은 브라우저 기본 UI도 테마에 맞게 렌더링되도록 한다.
- 색상, 폰트 크기 등은 CSS 커스텀 프로퍼티(`:root` 변수)로 정의해서 라이트/다크 테마 전환이 쉽게 되도록 한다.
- **모바일 반응형 필수.** 뷰포트 메타 태그, 유동적인 레이아웃(flexbox/grid), 상대 단위(rem, %, vw) 사용. 작은 화면에서 폰트/여백이 깨지지 않는지 항상 확인한다.
- 폰트는 시스템 폰트 스택을 기본으로 하되, 필요 시 웹폰트는 최소한으로만 추가한다.
- 코드 블록에는 가독성 좋은 monospace 폰트와 다크모드에서도 대비가 적절한 배경색을 사용한다.

## 접근성 (WCAG 2.2 AA)

- 라이트/다크 모드 각각에서 본문 텍스트는 배경과 **4.5:1**, 큰 텍스트(18pt+ 또는 14pt+ bold)는 **3:1** 이상의 명암비를 확보한다. 색상 변수를 바꿀 때마다 대비를 다시 확인한다.
- `<header>`, `<nav>`, `<main>`, `<article>`, `<time datetime="...">` 등 시맨틱 태그를 실제 의미에 맞게 사용한다. `<div>` 남용을 피한다.
- 모든 인터랙티브 요소(링크, 버튼, 테마 토글 등)는 키보드 포커스 시 라이트/다크 양쪽 배경에서 잘 보이는 아웃라인을 갖는다 (`outline` 제거 금지).
- 이미지에는 의미 있는 `alt` 텍스트를 붙인다. 장식용 이미지는 `alt=""`.

## SEO / 메타데이터

- 모든 페이지의 `<head>`에 `<meta name="description">`을 포함한다. 글 페이지(`post.html`)는 `posts.json`의 `summary` 값을 설명으로 사용한다.
- Open Graph 태그(`og:title`, `og:description`, `og:type`, `og:image`, `og:url`)와 Twitter Card 태그(`twitter:card` 등)를 최소한으로 채운다. 소셜 공유 시 미리보기가 깨지지 않도록 `og:image`는 실제 존재하는 이미지를 가리켜야 한다.
- 각 글에 `<link rel="canonical">`을 지정해 중복 URL 문제를 피한다.
- 배포(GitHub Pages 등) 시점에는 `sitemap.xml`과 RSS 피드(`feed.xml`) 추가를 고려한다 — 다만 지금은 정적 파일을 손으로 관리하는 프로젝트이므로, 글이 늘어나 필요성이 명확해지기 전까지 미리 만들지 않는다.
- 배포 도메인은 `https://lechahu12.github.io/my-blog/`로 확정. `canonical`/`og:url`은 이 값을 기준으로 한다.
- `og:image`/`twitter:image`는 실제 브랜드 이미지가 생기기 전까지 넣지 않는다. 빈 값이나 자리표시자 이미지를 채우지 않는다.
- **한계**: 카카오톡·페이스북·X 등 소셜 크롤러는 JS를 실행하지 않는다. `post.html?slug=...` 단일 템플릿 구조상 `js/post-render.js`가 갱신하는 글별 메타데이터는 크롤러에는 보이지 않고, 사이트 공통 메타데이터만 노출된다. 이를 해결하려면 빌드 단계(정적 프리렌더)가 필요한데, 이는 "빌드 도구 없음" 원칙과 정면으로 부딪히므로 실제로 필요해지기 전까지는 만들지 않는다.

## 성능

- `<img>`에는 항상 `width`/`height`(또는 `aspect-ratio`)를 명시해 레이아웃 시프트(CLS)를 방지한다.
- 화면 최상단에 보이는 이미지(LCP 후보)에는 `loading="lazy"`를 걸지 않는다. 스크롤해야 보이는 이미지에만 `loading="lazy"`를 사용한다.
- 시스템 폰트 스택을 쓰는 한 웹폰트 로딩 이슈는 발생하지 않는다 — 이 프로젝트의 현재 폰트 전략(시스템 폰트 우선)을 유지하는 것 자체가 성능 최적화다. 웹폰트를 추가하게 되면 `font-display: swap` 또는 `optional`을 지정한다.
- 불필요한 스크립트/의존성을 추가하지 않는 것이 이 프로젝트의 최우선 성능 전략이다 (빌드 도구 없이도 이미 빠르다는 점을 해치지 않는다).

## 마크다운 문법 지원 범위

- 이 프로젝트의 파서(`js/markdown.js`)는 [CommonMark](https://commonmark.org/) 전체 스펙을 목표로 하지 않는다. 다음 실용적인 서브셋만 안정적으로 지원하면 충분하다: 제목(`#`~`######`), 문단, 굵게/기울임, 인라인 코드, 코드 블록(펜스), 링크, 이미지, 순서/비순서 리스트, 인용구.
- 표(테이블), 중첩 리스트, 각주, 취소선 등은 GFM(GitHub Flavored Markdown) 확장 문법이며 CommonMark 코어에도 없다 — 필요해지기 전까지는 구현하지 않는다.
- 새로운 문법을 추가할 때는 CommonMark 스펙을 참고해 애매한 케이스(예: 리스트 항목 안의 문단, 중첩된 강조)의 동작을 결정하되, 파서 자체는 계속 직접 구현한 경량 버전을 유지한다 (외부 라이브러리 도입 금지 원칙은 유지).

## 기능 추가 시점

아래 기능들은 필요성이 실제로 생기기 전까지 미리 만들지 않는다 (불필요한 추상화 금지 원칙):

| 기능 | 만들 시점 |
|---|---|
| 태그 UI/필터링 | 글 5개 이상 + 반복되는 태그 2개 이상일 때 |
| 목차(TOC) | 글 하나에 제목이 3개 이상이거나 실질적으로 긴 글이 나올 때 |
| 코드 문법 하이라이팅 | 비trivial한 코드가 들어간 글이 나올 때 (`js/markdown.js`가 이미 `class="language-x"`를 코드블록에 붙이므로 훅은 준비돼 있음). 구현 시에도 라이브러리 없이 직접 만든 경량 토크나이저를 쓴다. |
| 클라이언트 검색 | 글 15~20개 이상일 때. `posts/search-index.json` 수동 관리 + 순수 JS 부분일치 매칭으로 구현하고 Lunr/Fuse/Pagefind 등 라이브러리는 도입하지 않는다. |
| `robots.txt` / `sitemap.xml` / `feed.xml` | 배포가 실제로 이뤄지고 검색엔진 노출이 필요해질 때 |
| 글별 정적 소셜 메타데이터(프리렌더 빌드 스크립트) | 카카오톡 등 소셜 공유가 실제로 중요해질 때 (위 SEO 섹션의 한계 참고). "빌드 도구 없음" 원칙에 가장 가깝게 닿는 예외이므로 신중히 결정한다. |

## 작업 방식

- UI/디자인 변경 후에는 반드시 브라우저에서 라이트/다크 모드, 데스크톱/모바일 크기를 직접 확인한다.
- 불필요한 추상화나 미래를 위한 과도한 설계를 피하고, 지금 필요한 기능만 구현한다.
- 주석은 "왜"가 비직관적일 때만 최소한으로 작성한다.
