# Review 지침 — 2048 게임

`/home/user/my-blog/apps/2048/` 에 구현된 2048 게임을 검증하는 Review 서브에이전트 지침이다. 너는 Build를 직접 수행하지 않았고, 결과물을 독립적으로 검증하는 역할이다.

## 검증 범위

1. **코드 리뷰**: `apps/2048/index.html`, `apps/2048/style.css`, `apps/2048/script.js`를 읽고 다음을 확인한다.
   - `spec.md`, `build-instructions.md`에 명시된 요구사항(격자 초기화, 랜덤 타일, 방향키+스와이프, 밀기/합치기 로직, 이중 병합 방지, 점수 계산, 게임오버/승리 판정, 재시작, localStorage 최고점수)이 실제로 구현되어 있는지.
   - JS 로직에 버그가 없는지 (예: 병합 로직이 반대 방향으로 밀 때도 올바른지, 인덱스 계산 오류, 무한 루프 가능성 등).
   - 블로그 본체 파일(`index.html`, `css/`, `js/`, `posts/`)이 전혀 수정되지 않았는지 `git status`/`git diff`로 확인.
   - 외부 라이브러리/CDN을 사용하지 않았는지.

2. **브라우저 동작 검증**: Playwright(Chromium은 `/opt/pw-browsers/chromium`에 사전 설치되어 있고 `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`, `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` 환경변수가 설정되어 있음)를 사용해 실제로 `apps/2048/index.html`을 로드하고:
   - 페이지가 정상 렌더링되는지 (콘솔 에러 없는지)
   - 방향키 입력(ArrowLeft/Right/Up/Down)을 보내 타일이 이동/병합되고 점수가 올라가는지
   - 재시작 버튼이 동작하는지
   - 최고 점수가 localStorage에 저장되고 페이지 리로드 후에도 유지되는지
   - 뷰포트를 모바일 크기(예: 375x667)로 바꿔도 레이아웃이 깨지지 않는지
   을 실제로 실행해서 확인한다. 필요하면 간단한 Playwright 스크립트를 작성해 실행하고, 끝나면 스크립트 파일은 정리(삭제)한다.

3. **문제 발견 시 직접 수정**: 코드 리뷰나 브라우저 테스트에서 버그/이슈를 발견하면, `apps/2048/` 폴더 안의 파일만 직접 수정해서 고쳐라. 블로그 본체 파일은 건드리지 마라.

## 산출물

- `/home/user/my-blog/review.md` 파일을 작성한다. 다음을 포함해야 한다:
  - 검증한 항목 체크리스트 (통과/실패)
  - 발견한 문제와 수정 내역 (있다면)
  - 최종 결론: "정상 동작 확인" 또는 남은 이슈
- review.md 이외에는 `apps/2048/` 폴더 밖의 파일을 수정하지 마라.

완료 후 요약을 보고해라.
