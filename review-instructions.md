# Review 지침 — 픽셀 아트 에디터

이 문서는 `/home/user/my-blog/apps/pixel-art-editor/`에 구현된 픽셀 아트 에디터를 검증하기 위한 Review 서브에이전트 전용 지침이다. 너는 Build 서브에이전트와 별개의 독립적인 검증자다.

## 범위

- 검증 대상: `/home/user/my-blog/apps/pixel-art-editor/index.html`, `style.css`, `script.js`
- 참고 문서: `/home/user/my-blog/spec.md` (원래 계획), `/home/user/my-blog/build-instructions.md` (Build 지침)
- **수정 권한**: 문제를 발견하면 `apps/pixel-art-editor/` 폴더 안의 파일만 직접 고쳐도 된다. 블로그 본체 파일이나 다른 앱 폴더는 절대 건드리지 않는다.
- 검증 결과는 `/home/user/my-blog/review.md`에 작성한다(기존 내용은 2048 리뷰이므로 덮어써도 된다).

## 검증 항목

### 1. 브라우저 실제 동작 확인 (Playwright 등으로 실행)
- `apps/pixel-art-editor/index.html`을 로컬 서버(예: `python3 -m http.server`) 또는 `file://`로 열어 실제 렌더링을 확인한다.
- 16x16 격자가 정상적으로 보이는지 확인한다.
- 팔레트에서 색상을 클릭한 뒤 캔버스를 클릭/드래그해서 실제로 도트가 찍히는지 확인한다.
- 여러 셀을 드래그로 연속해서 칠해지는지 확인한다(pointermove 처리).
- 지우개 버튼을 누르고 칠해진 셀을 지워지는지 확인한다.
- "전체 지우기" 버튼 동작 확인(확인창 처리 포함).
- 커스텀 색상 선택기(`input[type=color]`)로 색을 바꾸고 칠해지는지 확인한다.
- "PNG로 저장" 버튼을 눌렀을 때 다운로드가 트리거되는지 확인한다(가능하면 Playwright의 download 이벤트로 실제 파일이 생성되는지, 파일이 PNG이고 투명 배경이 유지되는지까지 확인).
- 모바일 화면 크기(예: 375x667)로 뷰포트를 좁혀서 레이아웃이 깨지지 않는지, 터치 이벤트(터치 스크린 에뮬레이션 또는 pointer 이벤트)로 그리기가 되는지 확인한다.
- 다크모드(`prefers-color-scheme: dark`)에서도 레이아웃/가독성에 문제가 없는지 확인한다.
- 콘솔에 에러가 없는지 확인한다.

### 2. 코드 리뷰
- spec.md에 명시된 모든 기능이 실제로 구현되어 있는지 대조한다.
- 자체 완결 원칙 위반 여부 확인: 외부 라이브러리/CDN을 쓰지 않았는지, 블로그 본체 파일을 참조하거나 의존하지 않는지.
- 접근성: `aria-label`, `aria-pressed`, 키보드 포커스 가능 여부.
- 명백한 버그, 엣지 케이스 누락 여부(예: 캔버스 밖으로 드래그, 빠른 연속 클릭 등).

### 3. 문제 발견 시
- 사소한 문제는 직접 수정한다(`apps/pixel-art-editor/` 범위 내에서만).
- 수정한 내용을 review.md에 기록한다.
- 구조적으로 큰 문제(설계 자체를 바꿔야 하는 수준)라면 수정하지 말고 review.md에 명확히 기록한 뒤 보고하라.

## review.md 작성 형식

- 검증 항목별 결과(통과/실패/수정함)
- 발견한 문제와 조치 내용
- 최종 결론: 배포(Embed) 가능 여부

## 완료 후

- review.md 내용을 요약해서 보고하라.
- 브라우저 테스트에 사용한 임시 서버 프로세스가 있다면 종료하라.
